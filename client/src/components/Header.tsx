import { HeaderContainer, HeaderTitle } from '../ui/MainUi';

export default function Header(): JSX.Element {
  return (
    <HeaderContainer>
      <HeaderTitle>Специализированные дисциплины</HeaderTitle>
    </HeaderContainer>
  );
}
